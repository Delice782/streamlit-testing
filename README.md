
# 🎬 Mini Movie Recommender (Streamlit)

A lightweight ML application built with Streamlit that provides:

- Top-N movie recommendations
- Movie search + similar movies
- Insight dashboard (genres, embeddings projection, popularity)

### 🚀 Run the app locally

```bash
pip install -r requirements.txt
streamlit run streamlit_movie_recommender.py
